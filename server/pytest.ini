[pytest]
# server/ (for the `app` package) and the repo root (for `tools`/`lib`) must
# both be importable — mirrors the runtime path model.
pythonpath = . ..
asyncio_mode = auto
testpaths = tests
addopts = -q
